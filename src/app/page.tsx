import { Navbar } from '@/components';
import { Button, Container } from '@mui/material';

export default function Home() {
    return (
        <main>
            <Navbar />
            <Container></Container>
        </main>
    );
}
